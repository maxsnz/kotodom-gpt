/**
 * Session cookie name
 */
export const SESSION_COOKIE_NAME = "sid";

/**
 * Default session TTL in seconds (24 hours)
 */
export const SESSION_TTL_SECONDS = 24 * 60 * 60;

/**
 * Session store injection token
 */
export const SESSION_STORE = Symbol("SESSION_STORE");
